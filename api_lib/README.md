# api_lib — Vercel support modules

Python helpers bundled into the four serverless entry points in `api/` via `includeFiles` in `vercel.json`.

**Not deployed as standalone functions** — only files directly under `api/*.py` (except support moved here) become Vercel serverless functions.
